import { Image, TextBlock } from "types.slint";

export global Logic {
    pure callback open-external(string);
    pure callback open-file() -> [Image];
    callback detect(Image);
    callback ocr(Image, [TextBlock]);
}
