import { Button, VerticalBox } from "std-widgets.slint";
import { AppMenuBar, AppMenu, AppMenuItem, AppMenuSeparator } from "menubar.slint";

export global Logic {
    pure callback open(string);
}

export component App inherits Window {
    max-width: 3840px;
    max-height: 2160px;
    preferred-width: 1024px;
    preferred-height: 768px;
    title: "Koharu";
    icon: @image-url("assets/Koharu_Halo.webp");

    VerticalLayout {
        AppMenuBar {
            AppMenu {
                title: "File";

                AppMenuItem {
                    title: "Open File...";
                }
            }

            AppMenu {
                title: "Help";

                AppMenuItem {
                    title: "Discord";
                    clicked => {
                        Logic.open("https://discord.gg/mHvHkxGnUY");
                    }
                }

                AppMenuItem {
                    title: "GitHub";
                    clicked => {
                        Logic.open("https://github.com/mayocream/koharu");
                    }
                }
            }
        }
    }
}
