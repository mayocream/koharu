import { Image, TextBlock } from "types.slint";

export global State {
    in-out property <Image> image;
    in-out property <[TextBlock]> text-blocks;
    in-out property <image> segment;
    in-out property <bool> auto-scaled: true;
}
