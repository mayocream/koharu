{
  "workspaces": [
    "ui/"
  ],
  "scripts": {
    "dev": "bun run --parallel dev:ui dev:native",
    "dev:ui": "bun run --cwd ui dev",
    "dev:native": "nodemon --watch Cargo.toml --watch Cargo.lock --watch crates --ext rs,toml,lock,wgsl,json --exec \"cargo run -p koharu --bin koharu\"",
    "build": "bun run --cwd ui build && cargo build -p koharu",
    "lint:ui": "bun run --filter ui lint",
    "test:ui": "bun run --filter ui test",
    "format": "oxfmt ui package.json .oxfmtrc.json .vscode",
    "format:check": "oxfmt --check ui package.json .oxfmtrc.json .vscode"
  },
  "devDependencies": {
    "git-cliff": "^2.13.1",
    "nodemon": "^3.1.14",
    "oxfmt": "^0.56.0"
  }
}
