# AGENTS.md

- Use `bun` instead of `npm` or `yarn` for package management and running scripts.
- Run `bun cargo *` instead of `cargo *` to load CUDA and cuDNN when testing koharu-ml crates.
- Do not use try-catch in TypeScript, use functional error handling.
